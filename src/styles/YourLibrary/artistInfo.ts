import styled from "@emotion/styled";

const StyledArtistInfo = styled.div`
  color: white;
  padding: 20px 30px;

  p {
    font-size: 20px;
    margin: 0;
  }

  .img-artist {
    height: 280px;
  }
  .artist-header {
    display: flex;
    align-items: end;
    padding: 40px 30px;
  }
  .text-card {
    margin-left: 40px;
  }
  .artist-name {
    font-size: 54px;
    margin: 10px 0;
  }
`;

export default StyledArtistInfo;
