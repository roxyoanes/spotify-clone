/* const initialState = {
  reminders: [],
};
const todos = (state = initialState, action) => {
  switch (action.type) {
    case "ADD_TODO":
      return {
        ...state,
        reminders: [...state.reminders, action.payload],
      };

    default:
      return state;
  }
};

export default todos; */
